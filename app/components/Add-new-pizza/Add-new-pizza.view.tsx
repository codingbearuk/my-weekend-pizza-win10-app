import React from 'react';

import { Container } from './Add-new-pizza.styles';

const View: React.FunctionComponent = (p) => {
  return (
    <Container>
      <h1>Add new pizza</h1>
    </Container>
  );
};

export default View;
